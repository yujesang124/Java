package Java0512;

public class ex00_day03Summary {

		public static void main(String args[]) {
			// 1. 랜덤메소드 : 0~1 수를 무작위로 발생시킨다. 0.00000 ~ 0.99999
			
			int rNum = (int)(Math.random() * 10) + 1;
			// 1~10 까지의 숫자 중 랜덤으로 발생
			// (int)(Math.random() * N) + S
			// 시작값 : S , 끝나는 값 : N
			
			double dNum = Math.random();
			System.out.println("dNum : " + dNum);
			// 3.6556
			// 5.7317
			// 9.0691
			int dice = (int)(Math.random() * 6) + 1;
			System.out.println("dice : " + dice);
			
			//2. 조건문 switch-case{
			//	case 변수값1:
			//	해당조건이 참일때 실행
			//	break;
			//	case 변수값2:
			//	해당조건이 참일때 실행
			//	break;
			// 	case 변수값3:
			//	해당조건이 참일때 실행
			// 	break;
			//  ~~~~~
			//	default:
			//	case에서 어떤 조건도 맞지 않을때 실행
			//	break;
			//	}
			// break가 실행될때까지 다음 case문을 실행한다.
			
			// 3. 반복문 For
			/*
			 
			 	for(초기화식;조건식;증감식){
			 	
			 		반복할 수행문
			 	}
			 
			 
			 */
			
				// }
			
			
	}
}
